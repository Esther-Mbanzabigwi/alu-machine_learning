name: prediction_app
description: A new Flutter project

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <3.0.3' # Specifies the Dart SDK version range

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4 # Add other dependencies as needed

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - images/ # Add any assets you want to include in your project
